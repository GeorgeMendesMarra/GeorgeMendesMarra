const precosDolar = [10, 20, 30];
const precosReal = [];

for (let i = 0; i < precosDolar.length; i++) {
    precosReal.push(precosDolar[i] * 5.5); // Multiplicando manualmente
}
console.log(precosReal);

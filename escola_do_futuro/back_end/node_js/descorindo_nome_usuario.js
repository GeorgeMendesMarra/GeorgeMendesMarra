// 'require' serve para importar módulos (ferramentas) que já vêm com o Node
const os = require('os'); // Importa o módulo de Sistema Operacional (OS)
console.log("Utilizador atual: " + os.userInfo().username);

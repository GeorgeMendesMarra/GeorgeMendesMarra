const linguagens = ['JavaScript', 'Python', 'C++', 'Java'];

// Percorre cada elemento diretamente, sem precisar do índice
for (const linguagem of linguagens) {
    console.log(`Linguagem de programação: ${linguagem}`);
}

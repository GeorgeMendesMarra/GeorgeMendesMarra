# import: Esta é a palavra-chave que importa um módulo em Python.
import sys
#stdout: Este é um objeto dentro do módulo sys que representa a saída padrão (geralmente o console).
sys.stdout.write("Hello, world!\n") # \n adiciona uma nova linha
# write(): Este é um método do objeto stdout que escreve uma string na saída padrão.
